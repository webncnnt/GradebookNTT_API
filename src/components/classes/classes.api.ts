import { Router } from 'express';

const router = Router();

router.post('/:id', () => {});

router.get('/:id', () => {});

router.patch('/:id', () => {});

router.get('/', () => {});

router.get('/');
